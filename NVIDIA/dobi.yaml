#
# Build a minimal docker image for a static go binary.
# See README.md for a full description.
#

meta:
  project: SuiSiann-NVIDIA

mount=giliau:
  bind: ./giliau/
  path: /giliau/
  read-only: true

mount=filelists:
  bind: ./filelists/
  path: /filelists/

mount=tshamsoo:
  bind: ./tshamsoo/
  path: /tshamsoo/
  read-only: true

mount=data:
  bind: ./data/
  path: /data/

image=tacotron2-kehsik:
  image: tacotron2-kehsik
  context: tacotron2-kehsik

job=tsuan-tacotron2-kehsik:
  use: tacotron2-kehsik
  mounts: [giliau, filelists, ]

image=tacotron2-khuanking:
  image: tacotron2-khuanking
  context: tacotron2-khuanking

job=tacotron2:
  use: tacotron2-khuanking
  command: |
    bash -eux -c '
      cp /tshamsoo/hparams.py .
      python train.py  --output_directory=/data/ --log_directory=log
    '
  mounts: [tshamsoo, giliau, filelists, ]

job=tsuan-waveglow-kehsik:
  use: tacotron2-kehsik
  command: |
    bash -eux -c '
      ls /giliau/ImTong/*.wav | tail -n+500 > waveglow_train_files.txt
      ls /giliau/ImTong//*.wav | head -n500 > waveglow_test_files.txt
    '
  mounts: [giliau, filelists, ]

image=waveglow-khuanking:
  image: waveglow-khuanking
  context: waveglow-khuanking

job=waveglow:
  use: waveglow-khuanking
  command: |
    bash -eux -c '
      cp /tshamsoo/config.json .
      python train.py -c config.json
    '
  mounts: [tshamsoo, giliau, filelists, ]

